{
  "name": "hairo-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run translate && vite",
    "compile": "vue-tsc --noEmit",
    "build": "npm run translate && npm run compile && vite build",
    "serve": "vite preview",
    "translate": "node scripts/generate-translations.js"
  },
  "dependencies": {
    "vue": "^3.0.5",
    "vue-router": "^4.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "typescript": "^4.7.4",
    "vite": "^4.3.9",
    "vue-tsc": "^0.38.1"
  }
}
